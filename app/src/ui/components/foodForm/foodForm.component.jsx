import { Modal } from '../modal/modal.component'
import './foodForm.css'

export function FoodForm({onClose}) {
    

    return (
        <Modal onClose={onClose}>
            
        </Modal>
    )
}